import { StyleSheet } from "react-native";

export const personalIdStyles = StyleSheet.create({
  input: {
    height: 40,
    borderWidth: 1,
    padding: 10,
    borderRadius: 10,
  },

  errorText: {
    alignSelf: "center",
    marginTop: -8,
    color: "red",
  },
});
